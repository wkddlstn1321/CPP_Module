#include<iostream>
#include<cstring>

int	main(int ac, char **av)
{
	if (ac != 2)
	{
		std::cout << "[./typeCast] [argument]" << std::endl;
		return (1);
	}
	char	*pos;
	strtod(av[1], &pos);
	{
		
	}
	if (strlen(av[1]) > 1);
		parse(av[1]);
	
	int			check;

	
	// n = line.find(".");
	// if (line.length() == 1 && )	
}
