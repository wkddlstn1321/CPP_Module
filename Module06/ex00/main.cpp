#include<iostream>

int	parse(std::string str)
{
	if (str.length() > 1)
	{
		while ()
		{
			;
		}
	}
}

int	main(int ac, char **av)
{
	if (ac != 2)
	{
		std::cout << "[./typeCast] [argument]" << std::endl;
		return (1);
	}
	std::string	line;
	line = static_cast<std::string>(av[1]);
	int			check;

	
	n = line.find(".");
	if (line.length() == 1 && )
	
	
	if (n == std::string::npos && line.length() == 1)
		;
	
}
